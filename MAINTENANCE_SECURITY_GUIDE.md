# 🛡️ دليل الصيانة الدورية وتحديثات الأمان والمكتبات (Security & Dependencies Playbook)
> **تاريخ الإعداد:** سبتمبر 2026  
> **الجدول الدوري الموصى به:** كل 3 إلى 6 أشهر  
> **الهدف:** الحفاظ على أمان النظام، سد الثغرات المكتشفة، وتحديث المكتبات تدريجياً دون كسر وظائف الموقع أو توقفه.

---

## 📋 الفهرس السريع
1. [قاعدة ذهبية قبل أن تبدأ (النسخ الاحتياطي)](#1-قاعدة-ذهبية-قبل-أن-تبدأ)
2. [خطوات فحص الثغرات الأمنية ومعالجتها (Security Audit)](#2-خطوات-فحص-الثغرات-الأمنية-ومعالجتها)
3. [بروتوكول تحديث المكتبات خطوة بخطوة (Safe Upgrade Protocol)](#3-بروتوكول-تحديث-المكتبات-خطوة-بخطوة)
4. [التحقق الإجباري الميكانيكي (Mechanical Verification)](#4-التحقق-الإجباري-الميكانيكي)
5. [تدوير المفاتيح السرية وتأمين البيئة (Secret & Token Rotation)](#5-تدوير-المفاتيح-السرية-وتأمين-البيئة)
6. [صيانة وتأمين سحابة Supabase وقواعد البيانات](#6-صيانة-وتأمين-سحابة-supabase-وقواعد-البيانات)
7. [جدول المهام الميداني (Checklist لكل 6 أشهر)](#7-جدول-المهام-الميداني-لكل-6-أشهر)
8. [خطة الطوارئ والاسترجاع السريع (Rollback Plan)](#8-خطة-الطوارئ-والاسترجاع-السريع)

---

## 1. قاعدة ذهبية قبل أن تبدأ
⚠️ **لا تبدأ أي تحديث على الكود الإنتاجي المباشر أو قبل عمل الآتي:**

1. تأكد من أنك تعمل داخل فرع جديد (Git Branch) مخصص للتحديثات:
   ```bash
   git checkout -b chore/maintenance-update-$(date +%Y%m%d)
   ```
2. تأكد من عمل Commit لجميع التغييرات الحالية حتى لا تفقد أي عمل غير محفوظ:
   ```bash
   git status
   ```
3. خذ نسخة احتياطية (Export Backup) لقاعدة البيانات من لوحة تحكم **Supabase -> Project Settings -> Database -> Backups**.

---

## 2. خطوات فحص الثغرات الأمنية ومعالجتها

### أ. فحص الثغرات في حزم npm:
قم بتشغيل الفحص التلقائي:
```bash
npm audit
```

### ب. قراءة نتائج الفحص:
- **Low / Moderate (منخفضة / متوسطة):** لا تستدعي القلق، غالباً في أدوات التطوير (DevDependencies).
- **High / Critical (عالية / حرجة):** تتطلب إصلاحاً فورياً لأنها قد تمثل خطورة استغلال.

### ج. الإصلاح التلقائي الآمن (Patch Fix):
يصلح فقط التحديثات التي لا تسبب تغييراً جذرياً في الـ API:
```bash
npm audit fix
```

> ⚠️ **تحذير شديد الأهمية:** إياك واستخدام أمر `npm audit fix --force` نهائياً؛ لأنه يقوم بترقية حزم كبرى وتغيير إصدارات رئيسية (Breaking Changes) مما قد يكسر بناء التطبيق.

---

## 3. بروتوكول تحديث المكتبات خطوة بخطوة

### الخطوة 1: معرفة الحزم التي تحتاج إلى تحديث
```bash
npm outdated
```
ستظهر لك ثلاثة أعمدة رئيسية:
- **Current:** الإصدار المثبت حالياً بجهازك.
- **Wanted:** أعلى إصدار متوافق في نفس الجيل (Patch/Minor) - **هذا آمن 100% للتحديث**.
- **Latest:** أحدث إصدار أصدره مطورو المكتبة (قد يحتوي على Major Changes ويحتاج لاختبار).

---

### الخطوة 2: تحديث الحزم الآمنة (Minor & Patch Releases)
تحديث جميع الحزم المتوافقة حسب ملف `package.json`:
```bash
npm update
```

---

### الخطوة 3: التعامل مع الحزم الحرجة في مشروع "عقار"

هناك مكتبات حساسة في هذا المشروع يجب تحديثها منفردة واختبارها بحذر:

| الحزمة | سبب الحساسية | طريقة التحديث الآمنة |
| :--- | :--- | :--- |
| `@supabase/supabase-js` | عميل قاعدة البيانات والـ Auth | حدّث أولاً: `npm i @supabase/supabase-js@latest`<br>ثم اختبر تسجيل الدخول واستعلامات العقارات. |
| `react` & `react-dom` | النواة الأساسية للواجهة | إذا كان الإصدار الحالي 18، لا ترقِ إلى 19 إلا بعد التأكد من دعم `@react-three/fiber` و `framer-motion`. |
| `vite` | بيئة البناء والخادم المحلي | حدّث مع فحص ملف `vite.config.ts`. |
| `lucide-react` | الأيقونات المستخدمة بالكامل | آمنة للتحديث، لكن تحقق من أسماء الأيقونات بعد التحديث. |
| `xlsx` | معالجة وتصدير ملفات الإكسل | تتبع ثغراتها باستمرار عبر `npm audit` لضمان أمان معالجة الجداول. |

لتحديث حزمة واحدة بعينها:
```bash
npm i <package_name>@latest
```

---

## 4. التحقق الإجباري الميكانيكي (Mechanical Verification)

بعد تنفيذ التحديثات، يجب أن تجري هذا الفحص الرباعي بالترتيب. إذا نجحت الاختبارات الأربعة، فأنت جاهز 100% للنشر:

### 1) فحص تطابق أنواع TypeScript:
```bash
npx tsc --noEmit
```
*(يجب أن ينتهي بدون أي أخطاء كود 0).*

### 2) فحص جودة وتنسيق الكود (Linting):
```bash
npm run lint
```

### 3) اختبار بناء حزمة الإنتاج (Production Build):
```bash
npm run build
```
*(يتحقق من أن الـ Tree-shaking والـ Minification لا يواجهان أي تضارب بين المكتبات).*

### 4) اختبار شامل لجميع سيناريوهات الموقع (Playwright E2E Tests):
```bash
npx playwright test e2e-scalability.spec.ts
```
*(يختبر تلقائياً: ضغط الصور، الأرشفة، مؤشرات الأداء، تنظيف الكاش، وتصفح العقارات والعملاء).*

---

## 5. تدوير المفاتيح السرية وتأمين البيئة (Secret & Token Rotation)

كل 6 أشهر، ينصح بتدوير المفاتيح لحماية الموقع في حال تسرب أي مفتاح بالخطأ:

### أ. فحص ملفات البيئة:
1. تأكد من أن ملف `.env` مدرج في `.gitignore` وأنه لم يُرفع على GitHub:
   ```bash
   git status --ignored
   ```
2. تأكد من وجود ملف `.env.example` محدث بالقوالب دون قيم حقيقية.

### ب. تدوير مفاتيح Supabase:
1. افتح **Supabase Dashboard -> Project Settings -> API**.
2. في قسم **Project API keys**:
   - يمكنك إعادة توليد الـ **JWT Secret** أو تغيير الـ **Service Role Key** إذا تم استخدامه في أدوات خارجية.
   - حدّث القيم في ملف `.env` المحلي وفي إعدادات الاستضافة (مثل Vercel / Netlify / Cloudflare).

### ج. تدوير مفتاح الذكاء الاصطناعي (Google Gemini API):
1. ادخل إلى Google AI Studio.
2. أنشئ مفتاحاً جديداً واحذف المفتاح القديم بعد تحديثه في إعدادات البيئة `VITE_GEMINI_API_KEY`.

---

## 6. صيانة وتأمين سحابة Supabase وقواعد البيانات

### أ. مراجعة سياسات أمان الصفوف (Row Level Security - RLS):
كل 6 أشهر، تأكد من عدم وجود أي جدول جديد تم إنشاؤه بدون تفعيل RLS:
- افتح **Supabase -> Authentication -> Policies**.
- تأكد أن جميع الجداول الأساسية (`properties`, `leads`, `teams`, `favorites`) عليها علامة **RLS Enabled**.
- تأكد أن المستخدم العادي لا يملك صلاحية حذف العقارات (`DELETE`)، وأنها مقصورة على الـ `admin`.

### ب. تنفيذ دالة أرشفة العملاء التلقائية:
قاعدة البيانات مزودة بدالة SQL لأرشفة الطلبات القديمة المكتملة أو المرفوضة التي مر عليها أكثر من 60 يوماً:
- ادخل إلى **Supabase -> SQL Editor**.
- شغل هذا الاستعلام لتنظيف الطلبات النشطة:
  ```sql
  SELECT public.archive_old_closed_leads();
  ```
  *(سيعيد لك عدد السجلات التي تمت أرشفتها تلقائياً لتفريغ الفهارس النشطة).*

### ج. فحص ترقية محرك PostgreSQL:
- تقدم Supabase تنبيهات في لوحة التحكم عند توفر تحديث أمني لمحرك PostgreSQL.
- اذهب إلى **Project Settings -> Infrastructure** واضغط على **Upgrade Database** إن وجد تنبيه أمان.

---

## 7. جدول المهام الميداني (Checklist لكل 6 أشهر)

قم بطباعة هذه القائمة أو تتبعها عند موعد الصيانة:

- [ ] إنشاء فرع عمل مؤقت `chore/deps-update-DATE`.
- [ ] أخذ Backup لقاعدة بيانات Supabase.
- [ ] تشغيل `npm audit` ومعالجة أي ثغرات بـ `npm audit fix`.
- [ ] فحص الحزم القديمة عبر `npm outdated` وتحديث الإصدارات الآمنة بـ `npm update`.
- [ ] تشغيل التحقق الصارم:
  - [ ] `npx tsc --noEmit`
  - [ ] `npm run build`
  - [ ] `npx playwright test e2e-scalability.spec.ts`
- [ ] تشغيل دالة الأرشفة `SELECT public.archive_old_closed_leads();` في Supabase.
- [ ] مراجعة ملف `.github/dependabot.yml` والتأكد من استقبال تنبيهات GitHub أسبوعياً.
- [ ] دمج الفرع (Merge) ورفعه للإنتاج (Deploy to Production).
- [ ] فتح الموقع وتجربة إضافة عقار وتصفح لوحة التحكم.

---

## 8. خطة الطوارئ والاسترجاع السريع (Rollback Plan)

في حال قمت بتحديث حزمة ما وأدى ذلك لخلل مفاجئ في الإنتاج:

1. **الرجوع لآخر Commit مستقر في Git:**
   ```bash
   git log --oneline -n 5
   # ارجع لآخر كوميت كان يعمل بنجاح
   git checkout main
   git reset --hard <LAST_STABLE_COMMIT_HASH>
   git push origin main --force
   ```
2. **استعادة المكتبات كما كانت بدقة (Clean Install):**
   ```bash
   rm -rf node_modules package-lock.json
   git checkout package.json package-lock.json
   npm install
   ```
3. **تفريغ كاش المستخدمين:**
   - قمنا بربط رقم النسخة في `src/lib/cacheManager.ts`.
   - فقط قم بزيادة رقم النسخة (مثلاً من `2.1.0` إلى `2.1.1`).
   - بمجرد دخول أي مستخدم للموقع، سيقوم الكود تلقائياً بحذف أي كاش قديم معلّق على جهازه وإعادة جلب النسخة المستقرة فوراً!

---
*تم إنشاء هذا الدليل لضمان بقاء منصة عقار سريعة، آمنة، وقابلة للتوسع لأكثر من سنتين دون مفاجآت تقنية.*
